<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Activate Your Account</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f4f4f4;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
      }
      .activation-container {
        background-color: white;
        padding: 20px;
        border-radius: 8px;
        box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
        max-width: 400px;
        text-align: center;
      }
      h1 {
        color: #333;
      }
      p {
        color: #666;
      }
      .code-box {
        display: inline-block;
        padding: 10px;
        margin: 20px 0;
        background-color: #f8f9fa;
        border: 1px solid #ccc;
        font-size: 24px;
        font-weight: bold;
        letter-spacing: 2px;
        color: #333;
      }
      .info {
        margin-top: 15px;
        font-size: 14px;
        color: #888;
      }
      .btn {
        display: inline-block;
        padding: 10px 20px;
        background-color: #28a745;
        color: white;
        border: none;
        border-radius: 5px;
        text-decoration: none;
        font-size: 16px;
        margin-top: 20px;
      }
      .btn:hover {
        background-color: #218838;
      }
    </style>
  </head>
  <body>
    <div class="activation-container">
      <h1>Activate Your Account</h1>
      <p>Hello <%= user.name %>,</p>
      <p>To activate your account, please use the following activation code:</p>

      <div class="code-box"><%= activationCode %></div>

      <p class="info">
        Enter this code on the activation page to verify your account. The code
        will expire in 5 Minute.
      </p>
    </div>
  </body>
</html>
